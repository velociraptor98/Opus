<svg width="240" height="64" viewBox="0 0 240 64" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Opus">
  <rect width="240" height="64" fill="#f3f2f2"/>

  <!-- OPUS, set in Archivo 800. Text rather than outlines, so the file stays
       editable; embed or convert before sending it anywhere Archivo isn't. -->
  <text x="12" y="36" fill="#201e1d"
        font-family="Archivo, system-ui, -apple-system, 'Segoe UI', sans-serif"
        font-size="34" font-weight="800" letter-spacing="-1.5">OPUS</text>

  <!-- the pipeline: three of five, the third one live -->
  <g transform="translate(12,44)">
    <rect width="20" height="5" fill="#201e1d"/>
    <rect x="23" width="20" height="5" fill="#201e1d"/>
    <rect x="46" width="20" height="5" fill="#ec3013"/>
    <rect x="69" width="20" height="5" fill="#d7d3d3"/>
    <rect x="92" width="20" height="5" fill="#d7d3d3"/>
  </g>
</svg>
